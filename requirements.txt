# Базовый набор — то, что ставится ВСЕГДА, в том числе на Render.
# Никакого torch/sentence-transformers здесь нарочно нет — см. requirements-local.txt.

# --- база данных ---
psycopg[binary]>=3.1
pgvector>=0.3

# --- LLM ---
groq>=0.11

# --- эмбеддинги/реранкинг через Cohere API (USE_COHERE=true) ---
cohere>=5.11

# --- lexical search для hybrid retrieval ---
rank-bm25>=0.2.2

# --- конфиг ---
python-dotenv>=1.0

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
